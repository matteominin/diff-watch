[project]
name = "diffnotifier"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Matteo Minin", email = "matteominin@yahoo.it" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
diffnotifier = "diffnotifier:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
